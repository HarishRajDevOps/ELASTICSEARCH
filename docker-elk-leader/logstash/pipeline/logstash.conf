input {
  elasticsearch {
    hosts => ["http://192.168.29.106:9200"]
    index => "*"  # Fetch all indices
    query => '{"query": {"match_all": {}}}'  # Fetch all documents
    schedule => "*/5 * * * * *"  # Fetch every 5 seconds
    docinfo => true  # Retrieve document metadata, including _id
  }
}
 output {
  elasticsearch {
    hosts => ["http://192.168.29.122:9201"]
    index => "%{[@metadata][_index]}"  # Reuse the original index name
    document_id => "%{[@metadata][_id]}"  # Use the original _id to avoid duplicates
    action => "create"  # Prevent overwriting existing documents
  }
}